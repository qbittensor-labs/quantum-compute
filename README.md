# Quantum-Compute Subnet

## Environment Variables
- You'll need a `.env` file with the `JOB_SERVER_URL` variable specified. This should be a string representation of the URL where the job server instance is running